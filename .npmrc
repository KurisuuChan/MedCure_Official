# Ensure proper React resolution
legacy-peer-deps=false
# The following settings are pnpm-specific and cause warnings when using npm.
# They are kept here commented out in case the project wants to use pnpm later.
# strict-peer-dependencies=false

# Use exact versions
save-exact=true

# Ensure proper module resolution
# node-linker=hoisted
